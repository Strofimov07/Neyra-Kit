{
  "mcpServers": {
    "firebase": {
      "command": "npx",
      "args": [
        "-y",
        "firebase-tools@latest",
        "mcp",
        "--dir",
        "{{FIREBASE_MCP_DIR}}",
        "--tools",
        "{{FIREBASE_MCP_TOOLS}}"
      ]
    }
  }
}
