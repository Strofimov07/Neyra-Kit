{
  "mcpServers": {
    "firebase": {
      "command": "npx",
      "args": [
        "-y",
        "firebase-tools@latest",
        "mcp",
        "--dir",
        "{{FIREBASE_MCP_DIR}}",
        "{{FIREBASE_MCP_SCOPE_FLAG}}",
        "{{FIREBASE_MCP_SCOPE}}"
      ]
    }
  }
}
